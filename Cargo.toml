[package]
name = "cloud-core"
version = "0.1.2"
edition = "2024"

[lib]
name = "cloud_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
